# Tasks: SemirPhone Mobile App

**Input**: Design documents from `specs/003-semir-phone-app/`  
**Plan**: [plan.md](plan.md) | **Spec**: [spec.md](spec.md) | **API Contract**: [contracts/api-contract.md](contracts/api-contract.md)  
**Tech stack**: Flutter 3.x / Dart 3.x (mobile) + DRF + simplejwt (backend API layer)  
**Total tasks**: 115 | **Sprints**: 0–7

## Format: `[ID] [P?] [Story?] Description — file path`

- **[P]**: Can run in parallel with other [P] tasks in the same phase
- **[US#]**: Maps task to user story from spec.md
- Each phase ends with a **sprint exit gate** — all gate items must pass before the next phase starts

---

## Phase 1: Setup — Project Scaffolding

**Purpose**: Create the Flutter project skeleton and CI infrastructure. No business logic.

- [X] T001 Create Flutter project `flutter create semir-phone --org com.semir --platforms ios,android` at repo root
- [X] T002 Add all dependencies to `semir-phone/pubspec.yaml`: dio, flutter_riverpod, riverpod_generator, flutter_secure_storage, local_auth, fl_chart, sentry_flutter, golden_toolkit, flutter_native_splash, intl, go_router — run `flutter pub get`
- [X] T003 [P] Create `semir-phone/Makefile` with targets: `run-ios`, `run-android`, `test`, `snapshot` (build targets added in Phase 10)
- [X] T004 [P] Create `.github/workflows/ci.yml` running `flutter test` on every push to any branch
- [X] T005 [P] Create `semir-phone/render/README.md` (gitignore `render/*` except README) — document that PNGs are generated by `make snapshot` and require senior QA review before any UI task is marked done

**Sprint exit gate:**
- [ ] `flutter doctor -v` shows no blocking issues (Flutter, Dart, Xcode, Android Studio)
- [ ] `flutter pub get` succeeds with no resolution conflicts
- [ ] `make test` runs (zero tests = passing) without error
- [ ] CI workflow file is valid YAML and passes lint check

---

## Phase 2: Foundational — Backend API (Sprint 0, Hard Blocker)

**Purpose**: Add JSON API layer to SemirDashboard. ALL mobile data-fetching depends on this.  
**⚠️ CRITICAL**: No mobile user story work can begin until this phase exits successfully.

- [X] T006 Add `djangorestframework`, `djangorestframework-simplejwt`, `django-cors-headers` to `SemirDashboard/requirements.txt` — run `pip install -r requirements.txt`
- [X] T007 Create `SemirDashboard/App/api/` package with `__init__.py`, `urls.py`, `serializers.py`, `views.py`, `permissions.py`
- [X] T008 Implement auth endpoints in `SemirDashboard/App/api/views.py`: `POST /api/v1/auth/token/` (login → returns access+refresh+permissions[]), `POST /api/v1/auth/token/refresh/` (silent refresh), `POST /api/v1/auth/logout/` (revoke)
- [X] T009 Configure simplejwt in `SemirDashboard/SemirDashboard/settings.py`: `ROTATE_REFRESH_TOKENS=True`, `BLACKLIST_AFTER_ROTATION=True`, access lifetime 1h, refresh lifetime 30d — add `rest_framework_simplejwt.token_blacklist` to `INSTALLED_APPS`
- [X] T010 Run `python manage.py makemigrations && python manage.py migrate` for the simplejwt blacklist table — verify migrations apply cleanly
- [X] T011 [P] Implement analytics endpoints in `SemirDashboard/App/api/views.py`: `GET /api/v1/analytics/sales/`, `/analytics/customer/`, `/analytics/coupon/`, `/analytics/shop-detail/`, `/analytics/customer-detail/` — reuse existing tab_functions + compute_cnv_breakdown; pre-format VND values as strings in serializers
- [X] T012 [P] Implement chart endpoints in `SemirDashboard/App/api/views.py`: `GET /api/v1/charts/sales/`, `/charts/customer/`, `/charts/coupon/` — return donut slices + trend series matching contract shape
- [X] T013 [P] Implement `GET /api/v1/analytics/shops/` dropdown endpoint in `SemirDashboard/App/api/views.py` — returns `{"shops": [...]}` ordered list
- [X] T014 Create `SemirDashboard/App/api_urls.py` wiring all 12 endpoints — add `path("api/v1/", include("App.api_urls"))` to `SemirDashboard/SemirDashboard/urls.py`
- [X] T015 Create `SemirDashboard/App/api/permissions.py` — DRF `HasPermission` class mirroring `@requires_perm` decorator; enforce on all analytics/chart endpoints; 401 without auth, 403 with wrong permission
- [X] T016 Configure `django-cors-headers` in `INSTALLED_APPS` + `MIDDLEWARE` in `SemirDashboard/SemirDashboard/settings.py`
- [X] T017 Verify all 12 endpoints via curl per Sprint 0 test gates in `plan.md`: correct JSON shape, 401 without auth, 403 wrong permission, 400 invalid date params, pre-formatted VND strings in table rows
- [X] T018 Run `cd SemirDashboard && python manage.py test tests -v 2` — all pre-existing web tests MUST pass (no regression from DRF addition)

**Sprint exit gate — Phase 2 is DONE when:**
- [ ] `POST /api/v1/auth/token/` returns `{access, refresh, permissions[]}` with valid credentials
- [ ] `POST /api/v1/auth/token/refresh/` issues new access token; old refresh token is blacklisted in DB
- [ ] `POST /api/v1/auth/logout/` returns 205; subsequent refresh attempt returns 401
- [ ] All analytics endpoints return 401 without auth header; 403 with wrong-permission token
- [ ] Table row values are pre-formatted strings (`"1,234,567"` not `1234567`; `"45.98%"` not `0.4598`)
- [ ] All pre-existing SemirDashboard tests pass (`python manage.py test tests -v 2`)
- [ ] `python manage.py runserver` starts without error with all new packages

---

## Phase 3: Foundational — Flutter Auth Core (Sprint 1)

**Purpose**: JWT token storage, HTTP interceptor, biometric, Riverpod auth state. Used by ALL user stories.  
**⚠️ CRITICAL**: All user story phases depend on this phase.

- [X] T019 [P] Create `semir-phone/lib/core/config/app_config.dart` — `BuildConfig` class reading `--dart-define` flags: `apiBaseUrl`, `tlsPin`, `tlsBackupPin`, `sentryDsn`, `environment`
- [X] T020 [P] Create `semir-phone/lib/core/theme/app_colors.dart` — all color constants matching web tokens: `primary = Color(0xFF0D6EFD)`, `allTimeCardBg`, `periodCardBg`, `textDark`, `textMuted`, `navBg`
- [X] T021 [P] Create `semir-phone/lib/core/theme/app_theme.dart` — `ThemeData` wiring `app_colors.dart` constants into `ColorScheme`, `textTheme`, `appBarTheme`
- [X] T022 Create `semir-phone/lib/core/auth/token_storage.dart` — `flutter_secure_storage` wrapper: `saveTokens(access, refresh)`, `readAccessToken()`, `readRefreshToken()`, `deleteAll()`, `saveBiometricEnabled(bool)`, `readBiometricEnabled()`
- [X] T023 Create `semir-phone/test/unit/token_storage_test.dart` — tests: write→read returns same value; delete→read returns null; both tokens stored independently; biometric flag persists
- [X] T024 Create `semir-phone/lib/core/auth/auth_service.dart` — `login(username, password)` → calls `POST /api/v1/auth/token/` → returns `UserSession`; `logout(refreshToken)` → calls `POST /api/v1/auth/logout/`; `refresh(refreshToken)` → calls `POST /api/v1/auth/token/refresh/` → returns new access token
- [X] T025 Create `semir-phone/test/unit/auth_service_test.dart` — mock Dio: login 200 → UserSession populated with correct fields; login 401 → throws `AuthException`; logout → calls revoke endpoint; refresh 200 → returns new token; refresh 401 → throws `SessionExpiredException`
- [X] T026 Create `semir-phone/lib/core/api/auth_interceptor.dart` — `QueuedInterceptor` with `_isRefreshing` bool flag + `_pendingRequests` queue: single 401 → 1 refresh call + retry; 3 concurrent 401s → exactly 1 refresh + retry all 3; refresh failure → throw `SessionExpiredException` and clear all pending
- [X] T027 Create `semir-phone/test/unit/auth_interceptor_test.dart` — tests: single 401 triggers 1 refresh; **concurrent 401 test**: fire 3 parallel requests with expired token → exactly 1 refresh call hits mock server → all 3 requests retried with new token; refresh failure → `SessionExpiredException` thrown, all 3 requests fail
- [X] T028 Create `semir-phone/lib/core/api/api_client.dart` — Dio singleton wired with `auth_interceptor`; base URL from `BuildConfig.apiBaseUrl`; `Authorization: Bearer <token>` header injected
- [X] T029 Create `semir-phone/lib/core/api/endpoints.dart` — all 12 API URL path constants as `static const String`
- [X] T030 Create `semir-phone/lib/core/auth/biometric_service.dart` — `local_auth` wrapper: `isBiometricAvailable()`, `authenticate()` → gates OS release of secure storage; NOT a backend auth call
- [X] T031 Create `semir-phone/test/unit/biometric_service_test.dart` — mock `local_auth`: unavailable → returns false; authenticate success → true; authenticate failure/cancel → false
- [X] T032 Create `semir-phone/lib/core/auth/auth_provider.dart` — Riverpod `AsyncNotifierProvider<UserSession?>`: `login()`, `logout()`, `restoreSession()` (reads tokens from secure storage on app launch)
- [X] T033 Create `semir-phone/test/unit/auth_provider_test.dart` — initial state null; after login → UserSession with correct permissions[]; after logout → null; restore session with stored valid tokens → UserSession
- [X] T034 Initialize Sentry in `semir-phone/lib/main.dart` — only when `BuildConfig.sentryDsn` is non-empty; configure `beforeSend` hook to strip any field matching phone/vip_id/invoice/token patterns (FR-006, FR-046)

**Sprint exit gate — Phase 3 is DONE when:**
- [ ] `flutter test test/unit/token_storage_test.dart` — all pass
- [ ] `flutter test test/unit/auth_service_test.dart` — all pass
- [ ] `flutter test test/unit/auth_interceptor_test.dart` — all pass, including **concurrent 401 test** (exactly 1 refresh call)
- [ ] `flutter test test/unit/biometric_service_test.dart` — all pass
- [ ] `flutter test test/unit/auth_provider_test.dart` — all pass
- [ ] `flutter run -d <iOS Simulator>` and `flutter run -d <Android Emulator>` — app launches (blank scaffold is fine)
- [ ] CI green (`make test`)

---

## Phase 4: User Story 1 — Secure Mobile Login (Priority: P1)

**Goal**: User can sign in, session persists across restarts, sign out wipes tokens, biometric opt-in works.

**Independent Test**: Install on fresh simulator → sign in → relaunch → still signed in → sign out → login screen. Invalid creds → generic error, no token stored.

- [X] T035 [US1] Create `semir-phone/lib/shared/widgets/error_banner.dart` — retry-able error state widget; `message` + `onRetry` callback; uses `AppColors.textDark`
- [X] T036 [US1] Create `semir-phone/lib/shared/widgets/loading_overlay.dart` — centered `CircularProgressIndicator` in primary blue; shown while async operations run (FR-025: never blank >1s)
- [X] T037 [US1] Create `semir-phone/lib/features/login/login_page.dart` — username/password fields; Sign In button; generic error display (FR-007: no field-level disclosure); offline detection → button disabled; calls `auth_provider.login()`
- [X] T038 [US1] Create `semir-phone/test/widget/login_page_test.dart` — valid input → calls auth_service.login(); invalid creds 401 → shows generic error, no token stored (verify token_storage mock not called with save); offline (mock connectivity) → Sign In button disabled; loading state → overlay visible during async call
- [X] T039 [US1] Create `semir-phone/lib/app.dart` — `MaterialApp.router` with GoRouter; auth guard: `redirect` checks `auth_provider` state → unauthenticated → `/login`, authenticated → `/home`; define all 10 routes
- [X] T040 [US1] Create `semir-phone/test/widget/router_test.dart` — unauthenticated → redirects to `/login`; authenticated → renders `/home`; **SC-009**: attempt to push `/sales` route without `sales.view` permission → auth guard blocks, shows "no access"; back-button on login page does not navigate to protected route
- [X] T041 [US1] Create `semir-phone/integration_test/login_flow_test.dart` — full E2E with mock backend: sign in → home renders; re-launch → session restored (restoreSession called); sign out → login screen; wrong password → generic error banner

**Sprint exit gate — Phase 4 is DONE when:**
- [ ] `flutter test test/widget/login_page_test.dart` — all pass
- [ ] `flutter test test/widget/router_test.dart` — all pass including SC-009 back-stack guard
- [ ] `flutter test integration_test/login_flow_test.dart` — all pass (mock backend)
- [ ] Manual test on **iOS Simulator**: sign in → home → kill app → relaunch → still signed in → sign out → login
- [ ] Manual test on **Android Emulator**: same flow
- [ ] CI green

---

## Phase 5: User Story 2 — Home Page Navigation (Priority: P1)

**Goal**: Authenticated user sees permission-gated card grid; cards navigate to correct pages; layout adapts to screen size.

**Independent Test**: Sign in as full-permission user → 8 cards visible. Sign in as sales-only user → 2 cards visible, layout reflows. Tap card → navigate to page.

- [X] T042 [US2] Create `semir-phone/lib/features/home/nav_card.dart` — card widget: blue top-border (4pt, `AppColors.primary`), icon + title + description + button; touch target ≥44pt; adapts to available width
- [X] T043 [US2] Create `semir-phone/test/widget/nav_card_test.dart` — card renders with correct border color; text matches provided title/description; tap button → callback fires; minimum height ≥44pt
- [X] T044 [US2] Create `semir-phone/lib/features/home/home_page.dart` — reads `UserSession.permissions`; shows cards matching permission gates from spec clarification Q2; single-column ≤375pt, 2-column ≥768pt; no empty slots on reflow (FR-012)
- [X] T045 [US2] Create `semir-phone/test/widget/home_page_test.dart` — full permissions → 8 cards; `sales.view` only → 2 cards, zero empty gaps in layout; `coupons.view` missing → Coupon Analytics card absent; tap card → correct route pushed; 375pt width → single column; 768pt width → 2-column grid
- [ ] T046 [US2] Generate and review golden snapshots — run `flutter test --update-goldens test/golden/` for `login.png`, `home_full_permissions.png`, `home_sales_only.png`; copy to `semir-phone/render/`; **QA review gate**: open each PNG and verify blue top-border on cards, no text truncation at 390pt, correct card count per permission set
- [X] T047 [US2] Create `semir-phone/integration_test/home_permissions_test.dart` — mock auth with different permission sets; verify correct cards rendered; verify navigation from each card

**Sprint exit gate — Phase 5 is DONE when:**
- [ ] `flutter test test/widget/nav_card_test.dart` — all pass
- [ ] `flutter test test/widget/home_page_test.dart` — all pass
- [ ] `flutter test test/widget/router_test.dart` — SC-009 still passes (no regression)
- [ ] **QA approval**: `render/login.png`, `render/home_full_permissions.png`, `render/home_sales_only.png` reviewed — blue top-border visible, layout correct, no text overflow
- [ ] Manual test: tap each visible card → navigates to correct page (or placeholder)
- [ ] CI green

---

## Phase 6: User Story 3 — Sales Analytics (Priority: P1)

**Goal**: Sales Analytics page with KPI cards, tab-switched tables, date/shop filters, pull-to-refresh, page cache.

**Independent Test**: Sales Analytics loads → Period + All-Time KPI cards (orange/blue tints) → switch tabs → tables render with sticky first column, horizontal scroll → filter change → data refreshes.

### Shared Widgets (used by US3–US7, built here first)

- [X] T048 [US3] Create `semir-phone/lib/shared/utils/vnd_formatter.dart` — `formatVnd(num)` → `"1,234,567,890"` (Vietnamese grouping); `formatPercent(num, {int decimals=2})` → `"45.98%"`
- [X] T049 [US3] Create `semir-phone/test/unit/vnd_formatter_test.dart` — `formatVnd(1234567890)` → `"1,234,567,890"`; `formatVnd(0)` → `"0"`; `formatPercent(45.984)` → `"45.98%"`; `formatPercent(100)` → `"100.00%"`
- [X] T050 [US3] Create `semir-phone/lib/shared/utils/date_utils.dart` — `DatePreset` enum + `computeDateRange(DatePreset)` → `{dateFrom, dateTo}`; `Last7Days`, `Last30Days`, `LastMonth`, `ThisMonth`, `Year2025`, etc.
- [X] T051 [US3] Create `semir-phone/test/unit/date_utils_test.dart` — `Last30Days` → correct dateFrom exactly 30 days before today; `Year2025` → `2025-01-01` to `2025-12-31`; `ThisMonth` → first day of current month to today; boundary: Dec 31 → `ThisMonth` ends Dec 31
- [X] T052 [US3] Create `semir-phone/lib/shared/widgets/data_table_widget.dart` — sticky first column via dual `SingleChildScrollView` with linked vertical `ScrollController`; left: fixed-width label column (vertical scroll only); right: data columns (horizontal + vertical scroll synchronized); accepts `List<String> headers` + `List<List<String>> rows`
- [X] T053 [US3] Create `semir-phone/test/widget/data_table_test.dart` — 200-row fixture: first column does not move on horizontal drag; horizontal drag moves right panel only; vertical scroll moves both panels in sync; no overflow errors; column header background = `AppColors.primary` + white text
- [X] T054 [US3] [P] Create `semir-phone/lib/shared/widgets/kpi_card.dart` — `KpiCard({required String label, required String value, required KpiVariant variant})` where `variant` is `allTime` (orange tint bg) or `period` (blue tint bg); all text `AppColors.textDark`; no hardcoded hex
- [X] T055 [US3] [P] Create `semir-phone/test/widget/kpi_card_test.dart` — allTime variant background = `AppColors.allTimeCardBg`; period variant = `AppColors.periodCardBg`; text color = `AppColors.textDark`; value and label render correctly
- [X] T056 [US3] [P] Create `semir-phone/lib/shared/widgets/section_header.dart` — solid `AppColors.primary` background, white text, matches web section card-header style (FR-018)
- [X] T057 [US3] [P] Create `semir-phone/lib/shared/widgets/dark_tabs.dart` — `DarkTabs({required List<String> tabs, required int selectedIndex, required ValueChanged<int> onTabSelected})` on `AppColors.navBg` container; inactive = white with opacity 0.3; active = white bg + `AppColors.navBg` text (FR-019)
- [X] T058 [US3] [P] Create `semir-phone/lib/shared/widgets/date_filter_bar.dart` — date range pickers + preset dropdown; `onFilterChanged(DateRangeFilter)` callback; enforces `dateFrom ≤ dateTo`
- [X] T059 [US3] [P] Create `semir-phone/lib/shared/widgets/shop_group_filter.dart` — dropdown: `"All"`, `"Bala Group"`, `"Semir Group"`, `"Others Group"`; `onChanged(String?)` callback
- [X] T060 [US3] [P] Create `semir-phone/lib/shared/widgets/pull_to_refresh.dart` — `RefreshIndicator` wrapper with primary color spinner; calls `onRefresh` async callback

### Sales Analytics Implementation

- [X] T061 [US3] Create `semir-phone/lib/features/analytics/sales/sales_service.dart` — `getSalesData(dateFrom, dateTo, shopGroup)` → `GET /api/v1/analytics/sales/` → deserializes to `SalesAnalyticsPayload`; throws `ApiException` on 4xx/5xx
- [X] T062 [US3] Create `semir-phone/test/unit/sales_service_test.dart` — mock Dio: happy path → `SalesAnalyticsPayload` with correct all_time_kpis + tabs; 401 → interceptor handles (not tested here); 500 → `ApiException` thrown; date params serialized as `YYYY-MM-DD`
- [X] T063 [US3] Create `semir-phone/lib/features/analytics/sales/sales_provider.dart` — `AsyncNotifierProvider<SalesAnalyticsPayload?>` with `DateRangeFilter` + `shopGroup` parameters; page cache: stores last payload + filter; re-fetch in background on pull-to-refresh
- [X] T064 [US3] Create `semir-phone/lib/features/analytics/sales/sales_page.dart` — 4 All-Time KPI cards (orange tint) + 8 Period KPI cards (blue tint); `DarkTabs` for BySeason/ByMonth/ByWeek/ByShop/ByGrade; `DataTableWidget` per tab; `DateFilterBar` + `ShopGroupFilter`; `PullToRefresh` wrapper; `LoadingOverlay` on initial load; `ErrorBanner` on failure
- [X] T065 [US3] Create `semir-phone/test/widget/sales_page_test.dart` — loading state: `LoadingOverlay` visible, no data widgets; data loaded: KPI cards rendered with correct values from fixture; error state: `ErrorBanner` visible with retry; tab switch: `DataTableWidget` updates to new tab data; filter change: provider called with new params
- [ ] T066 [US3] Generate golden snapshots — `semir-phone/render/sales_analytics.png`, `render/sales_loading.png`, `render/sales_error.png`; **QA review gate**: orange-tint all-time cards, blue-tint period cards, primary blue table headers, sticky column visible in screenshot, section headers solid blue + white text
- [X] T067 [US3] Create `semir-phone/integration_test/sales_analytics_test.dart` — mock backend (mock Dio): filter change → API called with correct `date_from`/`date_to` params; tab switch → correct tab data displayed; pull-to-refresh → re-fetch called; error response → error banner shown; cache: navigate away and back → cached data shown immediately
- [ ] T068 [US3] Manual parity check (documented, not automated) — open Sales Analytics with `date_from=2025-01-01&date_to=2025-12-31` on mobile + same filter on web; verify 3 KPI values match exactly (SC-003)

**Sprint exit gate — Phase 6 is DONE when:**
- [ ] `flutter test test/unit/vnd_formatter_test.dart` — all pass
- [ ] `flutter test test/unit/date_utils_test.dart` — all pass
- [ ] `flutter test test/widget/data_table_test.dart` — all pass; FPS verified ≥50 at 200 rows in Flutter DevTools
- [ ] `flutter test test/widget/kpi_card_test.dart` — all pass
- [ ] `flutter test test/unit/sales_service_test.dart` — all pass
- [ ] `flutter test test/widget/sales_page_test.dart` — all pass (loading + data + error states)
- [ ] `flutter test integration_test/sales_analytics_test.dart` — all pass (mock backend)
- [ ] **QA approval**: `render/sales_analytics.png`, `render/sales_loading.png`, `render/sales_error.png` reviewed
- [ ] Manual parity: ≥3 KPI values confirmed matching web for same filter
- [ ] CI green

---

## Phase 7: User Story 4 — Customer Analytics (Priority: P2)

**Goal**: Customer Analytics page with POS/CNV KPI cards, Registration Breakdown + Customer Comparison tabs.

**Independent Test**: Open Customer Analytics → 8 KPI cards (POS + CNV totals) → switch Registration Breakdown tabs → switch Customer Comparison tabs → numbers match web for same date filter.

*Note: TLS certificate pinning is implemented in this phase (moved from Sprint 6) so it is tested against real API calls before the app is feature-complete.*

- [X] T069 [US4] Update `semir-phone/lib/core/api/api_client.dart` — add `SecurityContext.setTrustedCertificatesBytes()` with CA/intermediate PEM from `assets/certs/ca.pem` + backup pin from `assets/certs/ca_backup.pem`; only active when `BuildConfig.tlsPin` is non-empty (debug builds skip pinning)
- [X] T070 [US4] Create `semir-phone/test/unit/api_client_test.dart` — when `tlsPin` empty → standard `HttpClient` used; when non-empty → custom `SecurityContext` applied; verify `HandshakeException` thrown with deliberately wrong pin (mock)
- [ ] T071 [US4] Manual TLS pinning test — build with real pin → all API calls succeed on iOS Simulator + Android Emulator; build with wrong pin → all calls fail with `HandshakeException` on both platforms
- [X] T072 [US4] Create `semir-phone/lib/features/analytics/customer/customer_service.dart` — `getCustomerData(dateFrom, dateTo)` → `GET /api/v1/analytics/customer/` → `CustomerAnalyticsPayload`
- [X] T073 [US4] Create `semir-phone/test/unit/customer_service_test.dart` — happy path → correct payload; 403 → `PermissionException`; malformed response → `ParseException`
- [X] T074 [US4] Create `semir-phone/lib/features/analytics/customer/customer_provider.dart` — `AsyncNotifierProvider<CustomerAnalyticsPayload?>` with date filter state
- [X] T075 [US4] Create `semir-phone/lib/features/analytics/customer/customer_page.dart` — All-Time KPI cards (orange tint) + Period KPI cards (blue tint); `DarkTabs` for Registration Breakdown (ByShop/ByMonth/ByGrade); `DarkTabs` for Customer Comparison (PosOnly/CnvOnly/Both); `DataTableWidget` per tab; `DateFilterBar`; error + loading states
- [X] T076 [US4] Create `semir-phone/test/widget/customer_page_test.dart` — KPI cards correct tints; Registration Breakdown tab switch; Customer Comparison tab switch; 403 response → "no access" message
- [ ] T077 [US4] Generate golden snapshot — `semir-phone/render/customer_analytics.png`; **QA review gate**: uniform orange/blue tints match sales page, dark-tabs pattern correct, no per-section accent colors

**Sprint exit gate — Phase 7 is DONE when:**
- [ ] TLS pinning verified manually on iOS Simulator + Android Emulator (correct pin = success, wrong pin = HandshakeException)
- [ ] `flutter test test/unit/api_client_test.dart` — all pass
- [ ] `flutter test test/unit/customer_service_test.dart` — all pass
- [ ] `flutter test test/widget/customer_page_test.dart` — all pass
- [ ] **QA approval**: `render/customer_analytics.png` reviewed — card tints match sales page
- [ ] CI green

---

## Phase 8: User Story 5 — Coupon Analytics (Priority: P2)

**Goal**: Coupon Analytics with uniform card style (no green/red/yellow), prefix filter, By Shop/Detail/Duplicates tabs.

**Independent Test**: Open Coupon Analytics → all stat cards in orange/blue uniform style → apply Coupon ID prefix filter → By Shop table updates → switch Detail/Duplicates tabs.

- [X] T078 [US5] Create `semir-phone/lib/features/analytics/coupon/coupon_service.dart` — `getCouponData(dateFrom, dateTo, shopGroup, prefix)` → `GET /api/v1/analytics/coupon/` → `CouponAnalyticsPayload`
- [X] T079 [US5] Create `semir-phone/test/unit/coupon_service_test.dart` — prefix filter → API called with `prefix=<value>` query param; null prefix → no prefix param; happy path deserialization
- [X] T080 [US5] Create `semir-phone/lib/features/analytics/coupon/coupon_provider.dart` — `AsyncNotifierProvider` with date filter + prefix state
- [X] T081 [US5] Create `semir-phone/lib/features/analytics/coupon/coupon_page.dart` — All-Time KPI cards (orange tint) + Period KPI cards (blue tint); NO green/red/yellow backgrounds; prefix filter input; `DarkTabs` for ByShop/Detail/Duplicates; `DataTableWidget` per tab; error + loading states
- [X] T082 [US5] Create `semir-phone/test/widget/coupon_page_test.dart` — stat cards use only `AppColors.allTimeCardBg` (orange) or `AppColors.periodCardBg` (blue) — test that no other background color is present; prefix filter change → provider called with new prefix; tab switch works
- [ ] T083 [US5] Generate golden snapshot — `semir-phone/render/coupon_analytics.png`; **QA review gate**: zero green/red/yellow KPI cards — all orange-tint (all-time) or blue-tint (period), exactly matching sales + customer pages

**Sprint exit gate — Phase 8 is DONE when:**
- [ ] `flutter test test/unit/coupon_service_test.dart` — all pass (prefix param test)
- [ ] `flutter test test/widget/coupon_page_test.dart` — all pass (no non-orange/blue card backgrounds)
- [ ] **QA approval**: `render/coupon_analytics.png` reviewed — uniform tints confirmed, no accent colors
- [ ] CI green

---

## Phase 9: User Story 6 — Shop Detail + Customer Detail (Priority: P2)

**Goal**: Shop Detail drills into a single shop (3 sections). Customer Detail looks up a customer by VIP ID or phone (masked display). PII never logged.

**Independent Test**: Shop Detail → pick shop → 3 sections load → uniform blue headers. Customer Detail → enter VIP ID → profile + masked phone + invoice history → enter unknown ID → "not found".

- [X] T084 [US6] Create `semir-phone/lib/features/analytics/shop_detail/shop_detail_service.dart` — `getShops()` → `GET /api/v1/analytics/shops/`; `getShopDetail(shop, dateFrom, dateTo)` → `GET /api/v1/analytics/shop-detail/` → `ShopDetailPayload`
- [X] T085 [US6] Create `semir-phone/test/unit/shop_detail_service_test.dart` — getShops returns list; getShopDetail happy path; empty shop list → empty array (not null); 403 → PermissionException
- [X] T086 [US6] Create `semir-phone/lib/features/analytics/shop_detail/shop_detail_provider.dart` — selected shop state + date filter state + `AsyncNotifierProvider<ShopDetailPayload?>`
- [X] T087 [US6] Create `semir-phone/lib/features/analytics/shop_detail/shop_detail_page.dart` — shop dropdown (loaded from `getShops()`); date filter; 3 lazy-loaded section cards (Sales / Customer / Coupon); each section card: `SectionHeader` (primary blue + white text) + `border-top: 4pt primary` (FR-023); KPI cards + `DataTableWidget` per section
- [X] T088 [US6] Create `semir-phone/test/widget/shop_detail_page_test.dart` — dropdown populated from mock shops list; empty list → "no shops" message; shop selected → provider called; 3 sections render with correct section headers; section card top-border is `AppColors.primary`
- [X] T089 [US6] Create `semir-phone/lib/features/analytics/customer_detail/customer_detail_service.dart` — `getCustomerDetail({vipId, phone})` → `GET /api/v1/analytics/customer-detail/` → `CustomerDetailPayload`; 404 → `NotFoundException`
- [X] T090 [US6] Create `semir-phone/test/unit/customer_detail_service_test.dart` — VIP ID search → correct query param; phone search → correct query param; 404 → NotFoundException; response phone field is already masked by API (test that raw phone from fixture is masked format)
- [X] T091 [US6] Create `semir-phone/lib/features/analytics/customer_detail/customer_detail_provider.dart` — search query state + `AsyncNotifierProvider<CustomerDetailPayload?>`
- [X] T092 [US6] Create `semir-phone/lib/features/analytics/customer_detail/customer_detail_page.dart` — VIP ID input + phone input (search by either); customer profile display (masked phone); grade badge; invoice history `DataTableWidget`; 404 → "customer not found" message
- [X] T093 [US6] Create `semir-phone/test/widget/customer_detail_page_test.dart` — VIP ID search → API called; phone shown is masked (`09x-xxx-x567` format, never full digits); 404 → "customer not found" banner; invoice table renders
- [X] T094 [US6] Create `semir-phone/test/unit/log_scrub_test.dart` — verify Sentry `beforeSend` hook strips fields: any field named or containing `phone`, `vip_id`, `invoice`, `token` → redacted before event sent (SC-008)
- [ ] T095 [US6] Generate golden snapshots — `semir-phone/render/shop_detail.png`, `render/customer_detail.png`, `render/customer_detail_not_found.png`; **QA review gate**: uniform blue section headers and 4pt top-borders, masked phone visible in customer detail, "not found" error state layout clean

**Sprint exit gate — Phase 9 is DONE when:**
- [ ] `flutter test test/unit/shop_detail_service_test.dart` — all pass
- [ ] `flutter test test/widget/shop_detail_page_test.dart` — all pass
- [ ] `flutter test test/unit/customer_detail_service_test.dart` — all pass
- [ ] `flutter test test/widget/customer_detail_page_test.dart` — all pass (masked phone test passes)
- [ ] `flutter test test/unit/log_scrub_test.dart` — **SC-008 passes**: all PII fields confirmed stripped from Sentry events
- [ ] **QA approval**: shop detail + customer detail + not-found golden PNGs reviewed
- [ ] Manual test: real customer VIP ID lookup → phone displayed is masked
- [ ] CI green

---

## Phase 10: User Story 7 — Charts (Priority: P3)

**Goal**: Donut + trend charts for Sales/Customer/Coupon; touch-interactive (tap slice → highlight table row).

**Independent Test**: Open Sales Analytics Charts → 8 donut cards rendered → tap slice → corresponding table row highlights and scrolls into view → switch to trend chart → line chart visible.

- [X] T096 [US7] Create `semir-phone/lib/features/charts/chart_service.dart` — `getSalesChartData()`, `getCustomerChartData()`, `getCouponChartData()` → call respective `/api/v1/charts/` endpoints → `ChartPayload`
- [X] T097 [US7] Create `semir-phone/test/unit/chart_service_test.dart` — happy path → `ChartPayload` deserialized; null trend in response → `ChartPayload.trend` is null; donut slice colors preserved as hex strings
- [X] T098 [US7] Create `semir-phone/lib/features/charts/donut_card.dart` — `fl_chart` `PieChart` wrapped in card; `title` + slice legend; `onSliceTapped(String label)` callback; size adapts to phone width; colors from API payload hex strings
- [X] T099 [US7] Create `semir-phone/test/widget/donut_card_test.dart` — renders with correct slice count; tap slice → `onSliceTapped` fires with correct label; null data → loading indicator; adapts width
- [X] T100 [US7] Create `semir-phone/lib/features/charts/chart_provider.dart` — `AsyncNotifierProvider<ChartPayload?>` per chart type; `selectedSliceLabel` state for highlight coordination
- [X] T101 [US7] [P] Create `semir-phone/lib/features/charts/sales_chart_page.dart` — grid of up to 8 `DonutCard`; optional trend `LineChart` below; `DataTableWidget` at bottom; tap donut slice → `selectedSliceLabel` updated → table row with matching label gets highlight background + `ScrollController.animateTo` scrolls into view
- [X] T102 [US7] [P] Create `semir-phone/lib/features/charts/customer_chart_page.dart` — same pattern as sales chart page
- [X] T103 [US7] [P] Create `semir-phone/lib/features/charts/coupon_chart_page.dart` — same pattern
- [X] T104 [US7] Create `semir-phone/test/widget/chart_page_test.dart` — donut grid renders correct count; tap slice → table row highlight background applied; `null` trend → no trend section in DOM; legend visible
- [X] T105 [US7] Create `semir-phone/test/widget/chart_interaction_test.dart` — tap donut slice label "M2-4 2025" → table row with label "M2-4 2025" has highlight background; `ScrollController` called with offset putting that row in viewport
- [ ] T106 [US7] Generate golden snapshots — `semir-phone/render/sales_charts.png`, `render/customer_charts.png`, `render/coupon_charts.png`; **QA review gate**: donut colors match web chart palette, trend chart readable at 390pt, highlight visible on tapped slice

**Sprint exit gate — Phase 10 is DONE when:**
- [ ] `flutter test test/unit/chart_service_test.dart` — all pass
- [ ] `flutter test test/widget/donut_card_test.dart` — all pass
- [ ] `flutter test test/widget/chart_page_test.dart` — all pass
- [ ] `flutter test test/widget/chart_interaction_test.dart` — tap→highlight test passes
- [ ] **QA approval**: all 3 chart golden PNGs reviewed
- [ ] CI green

---

## Phase 11: Polish, Store Assets & Release (Sprint 7)

**Purpose**: App icon, splash screen, store assets, signing, DEPLOYMENT.md, real-device validation, store submission.

- [ ] T107 Configure `flutter_native_splash` in `semir-phone/pubspec.yaml` and run `dart run flutter_native_splash:create` — primary blue background + white app logo; verify on iOS Simulator (no white flash) + Android Emulator
- [ ] T108 Create app icon at `semir-phone/assets/icons/app_icon.png` (1024×1024, no alpha); configure `flutter_launcher_icons` in `pubspec.yaml` for iOS + Android adaptive icon (foreground + background layers); run `dart run flutter_launcher_icons`
- [X] T109 Create `semir-phone/store-assets/` folder: `app-name.txt` (`"SemirPhone"`), `description-vi.txt` (store description in Vietnamese), `keywords.txt`, `privacy-policy-url.txt`
- [ ] T110 [P] Generate store screenshots for iOS — run app on iPhone 15 Pro Simulator (6.7") + iPhone 8 Simulator (5.5"); capture at least 3 screens per device (login, home, sales analytics); save to `semir-phone/store-assets/screenshots/ios-6.7/` and `ios-5.5/`
- [ ] T111 [P] Generate store screenshots for Android — run app on Pixel 7 Emulator (1080×2340); capture same 3 screens; save to `semir-phone/store-assets/screenshots/android-1080x2340/`
- [X] T112 Create `semir-phone/fastlane/Fastfile` with `ios beta` lane (build IPA + upload to TestFlight) and `android beta` lane (build signed AAB + upload to Play Internal Testing)
- [X] T113 Create `semir-phone/fastlane/Appfile` (Bundle ID: `com.semir.semirphone`) and `semir-phone/fastlane/Matchfile` (iOS cert sync config)
- [X] T114 Add `build-ios` and `build-android` targets to `semir-phone/Makefile` — call `fastlane ios beta` and `fastlane android beta` respectively
- [X] T115 Write `semir-phone/DEPLOYMENT.md` — complete 10-step guide per FR-045: Apple Developer setup → cert issuance → App Store Connect record → IPA build + upload → TestFlight internal → App Store submission checklist → Google Play Console setup → AAB build → Internal Testing upload → Production promotion → update flow
- [X] T116 Update `semir-phone/README.md` final pass — verify all commands in FR-038 are accurate, all prerequisite versions listed, quickstart verified
- [ ] T117 SC-010 validation — fresh developer (not involved in building) follows README from checkout to running simulator + `make test` + `make snapshot`; must complete within 2 hours; fix any blocking step before closing
- [ ] T118 SC-001 validation — cold start timing: launch app on mid-tier Android (Pixel 6a equivalent) with valid cached session; measure seconds from app icon tap to home page fully rendered; MUST be ≤3 seconds
- [ ] T119 SC-002 validation — throttled network: iOS Simulator with Network Link Conditioner set to 10 Mbps download / 5 Mbps upload; open Sales Analytics; measure seconds from tap to data rendered; MUST be ≤4 seconds
- [ ] T120 Install on physical iOS device via TestFlight — full US1+US2+US3 flow; biometric opt-in; verify no crashes; data matches web
- [ ] T121 Install on physical Android device via Play Internal Testing — same validation as TestFlight

**Sprint exit gate — Phase 11 is DONE when:**
- [ ] `make build-ios` succeeds on clean checkout with correct signing credentials
- [ ] `make build-android` succeeds with keystore env vars set
- [ ] App installed on ≥1 physical iOS + ≥1 physical Android device
- [ ] Full US1→US2→US3 flow validated on real devices
- [ ] SC-001: cold start ≤3s confirmed on mid-tier Android
- [ ] SC-002: data load ≤4s under throttled 10 Mbps network
- [ ] SC-010: fresh developer onboarding completed within 2 hours
- [ ] `store-assets/screenshots/` contains correct-dimension PNGs for all required sizes
- [ ] App Store + Google Play submissions prepared; both stores ready for review submission
- [ ] `make test` passes on clean checkout

---

## Dependencies & Execution Order

### Phase Dependencies

```
Phase 1 (Setup)
    └── Phase 2 (Backend API) ← hard blocker; no mobile work until this exits
            └── Phase 3 (Flutter Auth Core) ← blocks all user story phases
                    ├── Phase 4 (US1: Login)
                    │       └── Phase 5 (US2: Home) ← needs auth guard from Phase 4
                    │               └── Phase 6 (US3: Sales) ← needs home navigation
                    │                       ├── Phase 7 (US4: Customer) ← shared widgets ready
                    │                       ├── Phase 8 (US5: Coupon)   ← shared widgets ready
                    │                       └── Phase 9 (US6: Shop+Customer Detail)
                    │                               └── Phase 10 (US7: Charts)
                    │                                       └── Phase 11 (Polish + Release)
```

### User Story Dependencies

| User Story | Depends On | Can Parallelize With |
|------------|------------|----------------------|
| US1 (Login) | Phase 3 (Auth Core) | — |
| US2 (Home) | US1 (auth guard) | — |
| US3 (Sales) | US2 (home navigation) | — |
| US4 (Customer) | US3 (shared widgets built) | US5, US6 |
| US5 (Coupon) | US3 (shared widgets built) | US4, US6 |
| US6 (Shop+Detail) | US3 (shared widgets built) | US4, US5 |
| US7 (Charts) | US3–US6 complete | — |

### Parallel Opportunities Within Each Phase

**Phase 3**: T019, T020, T021 can run in parallel (independent files)  
**Phase 6 (US3)**: T054, T055, T056, T057, T058, T059, T060 can run in parallel (all shared widgets are independent)  
**Phase 10 (US7)**: T101, T102, T103 can run in parallel (separate chart pages)  
**Phase 11**: T110, T111 can run in parallel (different device screenshots)

---

## Parallel Execution Example: Phase 6 (US3 Shared Widgets)

```bash
# These 7 tasks can run simultaneously:
Task T054: Create kpi_card.dart
Task T055: Create kpi_card_test.dart
Task T056: Create section_header.dart
Task T057: Create dark_tabs.dart
Task T058: Create date_filter_bar.dart + date_utils.dart
Task T059: Create shop_group_filter.dart
Task T060: Create pull_to_refresh.dart
```

---

## Implementation Strategy

### MVP (US1 + US2 + US3 only — Phases 1–6)

1. Phase 1: Setup → 2. Phase 2: Backend API → 3. Phase 3: Auth Core → 4. Phase 4: US1 Login → 5. Phase 5: US2 Home → 6. Phase 6: US3 Sales Analytics  
**STOP and VALIDATE**: All sprint exit gates for Phases 1–6 pass → this is a shippable internal build.

### Incremental Delivery

- After Phase 6: shippable MVP (Login + Home + Sales Analytics)
- After Phase 7+8: adds Customer + Coupon Analytics
- After Phase 9: adds Shop Detail + Customer Detail
- After Phase 10: adds Charts
- After Phase 11: App Store + Google Play ready

### Notes

- Every sprint exit gate must pass before the next phase starts — no skipping
- [P] tasks = different files, no blocking dependencies within the same phase
- Golden snapshot QA review is a **hard gate** — PNG must be opened and visually approved before marking the task done
- `make test` must pass at the end of every phase; CI must stay green throughout
- Commit after each completed phase exit gate
